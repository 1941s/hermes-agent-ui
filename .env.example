# Local development defaults (non-Docker).
# Copy to `.env` at repo root before `pnpm dev:web`.

NEXT_PUBLIC_AGENT_WS_URL=ws://localhost:8000/ws/agent
NEXT_PUBLIC_AGENT_HTTP_URL=http://localhost:8000
NEXT_PUBLIC_AGENT_AUTH_TOKEN=
